# 터널 막장면 기반 3차원 절리면 최적 복원 알고리즘 (Fracture Reconstruction Algorithm)

본 문서는 B 방향 파이프라인(Direction B: Inverse Reconstruction)에 탑재된 핵심 역산 알고리즘들의 수학적, 이론적 원리를 정리한 문서입니다.

## 1. 3차원 디스크(DFN)와 2D 굴착면의 교차 추출 알고리즘 (Synthetic Trace Extraction)
실제 3D DFN 원판 모델을 굴착 단면(x = Xf)에서 관측되는 형태(Trace)로 묘사하기 위해 사용하는 해석기하학적 투영 알고리즘입니다.

1. **유효성 필터링**: 
   - 디스크 중심 좌표 $C(C_x, C_y, C_z)$, 법선 벡터 $N(N_x, N_y, N_z)$, 반경 $R$.
   - 굴착면(x = Xf)과 디스크 중심 사이의 X축 이격 거리 $\Delta x = X_f - C_x$.
   - 디스크 평면을 따라 막장면까지 닿는 최단 반경 거리 $d = |\Delta x| / \sin(\alpha)$, 이때 $\sin(\alpha) = \sqrt{1 - N_x^2}$.
   - $d \le R$ 인 디스크에 대해서만 막장면에서 교차 선분이 발생합니다.

2. **교차 선분 복원**:
   - 교차선의 평면 내 방향 벡터 $L = (0, -N_z, N_y)$ 
   - 교차 선분의 막장면 내 중심점 $P_c$: 디스크 중심 $C$를 절리면의 경사를 따라 $x = X_f$가 될 때까지 이동시켰을 때의 y, z 좌표.
   - 피타고라스 역산으로 도출한 실제 교차선의 절반 길이 $h = \sqrt{R^2 - d^2}$.
   - 양 끝점 산출 $(P_{1y}, P_{1z})$ 및 $(P_{2y}, P_{2z})$.

3. **터널 내부 폴리곤 클리핑**:
   - `Shapely`의 Polygon 객체 교차(Intersection) 연산을 통하여, 위에서 구해진 무한정 길이의 선분 혹은 터널 외곽으로 벗어나는 선분을 엄격하게 **터널 내부에서만 관측 가능한 구간**으로 잘라냅니다.

## 2. 막장면 연속 트레이스 추적 매칭 (Multi-frame Trace Tracking)
연속된 막장면(Face $n-1$, Face $n$)에서 측정된 2D Trace 선분 중 서로 "같은 3D 절리의 흔적"인지 판단하여, **최소 3개 이상의 막장면에서 연속적으로 연장되는(Tracked) 궤적**만을 진짜 절리로 솎아내는 Heuristic(경험적) 매칭 추적 알고리즘입니다.

- 인접 면의 $Trace_a$와 $Trace_b$ 간의 **Cost Function(비용 함수)**을 산출해 최소 비용을 짝짓습니다.
- **거리 편차(Distance)**: $\Delta y_{mid}^2 + \Delta z_{mid}^2$ (두 선분의 중심점 간 2D 프로젝션 거리)
- **크기 편차(Scale)**: $|Length_A - Length_B|$
- **배향 편차(Orientation)**: 2D 각도의 사인 오차 $\Delta \theta$
- Cost 점수 임계값을 넘는 경우 아예 별개의 절리로 규정하여 단절된 Track으로 취급합니다.
- 매 굴착면마다 기존 매칭 트랙에 꼬리물기를 수행하며, 최종적으로 끊기지 않고 **길이가 3 이상인 궤적(즉, 3개 이상의 굴착면을 관통한 자국)**만 유효한 역산 후보로 살려둡니다.

## 3. 3차원 다항 점군 평면 단위 재구성 알고리즘 (Multi-Point SVD Plane Reconstruction)
3장 이상의 막장면에 걸쳐 추적된(Matched) 궤적 내의 **모든 2D Trace 선분들의 양 끝점**으로부터 공간 상의 단일 3D 평면 방정식(중심점, 법선 벡터)을 복원하는 핵심 최적화 연산입니다.

1. **3차원 공간 투영 (Lifting)**:
   - 최소 3장의 Face 간 간격(예: x=0.0m, x=3.0m, x=6.0m)과 추출된 다수의 선분 양 끝점을 활용하여 공간 상에 최소 $2 \times N (N \ge 3)$ 개의 3D 대응점(Point Cloud) 행렬 $P$을 형성합니다.

2. **무게 중심 (Centroid) 계산**:
   - 도출된 점군 $P$의 평균값을 취하여 평면을 대표하는 3차원 중심점(Centroid, $X_c, Y_c, Z_c$)을 설정합니다.

3. **최소자승 기반 공간 법선 벡터 도출 (SVD Fitting)**:
   - 각 점의 위치에서 중심점을 뺀 영점 편차 행렬(Mean-Centered Matrix) $A = P - Centroid$ 를 도출합니다.
   - 특이값 분해(Singular Value Decomposition)를 수행합니다. $A = U \cdot \Sigma \cdot V^T$
   - 결과로 나온 직교 행렬 $V^T$의 마지막 행 벡터(주성분 분석 관점에서 분산이 가장 작아 평면에 직교하는 축)를 해당하는 짝의 **최적 3D 법선 벡터(Normal Vector)**로 추출합니다.
   - 마지막으로 터널의 굴진 진행 방향(통상 +X)을 고려하여 법선 벡터의 부호가 좌/우 한 쪽으로 일관되도록 정규화(Normalization)하여 복원을 종료합니다.
